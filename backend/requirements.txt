# ---------------------------------------------------------------------------
# 学生助教管理平台 - Python 依赖（部署到 PythonAnywhere 等环境时使用）
# ---------------------------------------------------------------------------
# 如何生成/更新本文件：
#   1. 在项目根目录打开终端，激活虚拟环境后进入 backend 目录：
#      cd backend
#   2. 生成当前环境所有包（含版本）： pip freeze > requirements.txt
#   3. 或只记录本项目直接依赖（推荐手写）：保留下面列表，按需增删版本号
# 部署到 PythonAnywhere 时：在 PA 的虚拟环境里执行 pip install -r requirements.txt
# ---------------------------------------------------------------------------

# Django 核心
Django==4.2.7
mysqlclient==2.2.0

# Django REST Framework（前后端分离 API）
djangorestframework==3.14.0
djangorestframework-simplejwt==5.3.0
django-cors-headers==4.3.0
django-filter==23.5

# 图片处理（用户头像、上传等）
Pillow==10.1.0

# 工具库
python-dateutil==2.8.2
APScheduler==3.10.1

# 环境变量（.env，部署必备）
python-dotenv==1.0.1

# Admin 后台美化
django-jazzmin==2.6.0

# 以下为可选：仅开发/调试时需要，部署到生产可注释掉以加快安装
# django-debug-toolbar==4.2.0
# drf-yasg==1.21.5

